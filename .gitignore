# ----------------------------------------------------------------------
# IGNORING ENVIRONMENT, SENSITIVE DATA, AND LOCAL SECRETS
# ----------------------------------------------------------------------
# The .env file contains sensitive database credentials.
.env
# Key files often created by Python IDEs
.idea/
.vscode/

# ----------------------------------------------------------------------
# PYTHON VIRTUAL ENVIRONMENTS AND PACKAGE MANAGEMENT
# ----------------------------------------------------------------------
# Virtual environment directories
/venv
/env
*.egg-info/
dist/
build/

# Package installation
*.pyc
__pycache__/
/htmlcov/
.tox/

# ----------------------------------------------------------------------
# DOCKER AND DATABASE ARTIFACTS
# ----------------------------------------------------------------------
# Log files generated by Docker
*.log
# Docker build context files
*.tar
*.zip
# Persistent volumes (Docker Compose creates a local directory for these)
db_data/
# Note: Docker Compose volumes are usually hidden, but if you map a local
# directory for MySQL data, it must be ignored.
# You can usually see the volume in 'docker volume ls', and this is often
# not necessary unless you are manually mounting a local volume path.

# ----------------------------------------------------------------------
# OPERATING SYSTEM FILES
# ----------------------------------------------------------------------
.DS_Store # macOS
Thumbs.db # Windows